name: CI

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  pr-title:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
    steps:
      - uses: amannn/action-semantic-pull-request@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  clippy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --all-targets -- -D warnings

  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --workspace
      - run: cargo test --workspace

  publish-dry-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo publish --dry-run -p treesitter-types
      - run: cargo publish --dry-run -p treesitter-types-macros
      - run: cargo publish --dry-run -p treesitter-types-c
      - run: cargo publish --dry-run -p treesitter-types-c-sharp
      - run: cargo publish --dry-run -p treesitter-types-cpp
      - run: cargo publish --dry-run -p treesitter-types-css
      - run: cargo publish --dry-run -p treesitter-types-elixir
      - run: cargo publish --dry-run -p treesitter-types-go
      - run: cargo publish --dry-run -p treesitter-types-haskell
      - run: cargo publish --dry-run -p treesitter-types-html
      - run: cargo publish --dry-run -p treesitter-types-java
      - run: cargo publish --dry-run -p treesitter-types-javascript
      - run: cargo publish --dry-run -p treesitter-types-json
      - run: cargo publish --dry-run -p treesitter-types-lua
      - run: cargo publish --dry-run -p treesitter-types-markdown
      - run: cargo publish --dry-run -p treesitter-types-php
      - run: cargo publish --dry-run -p treesitter-types-python
      - run: cargo publish --dry-run -p treesitter-types-regex
      - run: cargo publish --dry-run -p treesitter-types-ruby
      - run: cargo publish --dry-run -p treesitter-types-rust
      - run: cargo publish --dry-run -p treesitter-types-scala
      - run: cargo publish --dry-run -p treesitter-types-swift
      - run: cargo publish --dry-run -p treesitter-types-toml
      - run: cargo publish --dry-run -p treesitter-types-typescript
      - run: cargo publish --dry-run -p treesitter-types-yaml

  codegen-freshness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: extractions/setup-just@v3
      - run: just generate
      - run: git diff --exit-code -- '*.rs' '*.json'

  integration:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        lang:
          - c
          - c-sharp
          - cpp
          - css
          - go
          - haskell
          - html
          - java
          - javascript
          - json
          - php
          - python
          - ruby
          - rust
          - scala
          - typescript
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: integration
      - uses: extractions/setup-just@v3
      - run: just integration-test-${{ matrix.lang }}

  ci:
    runs-on: ubuntu-latest
    if: always()
    needs: [pr-title, fmt, clippy, build-and-test, publish-dry-run, codegen-freshness, integration]
    steps:
      - name: Check results
        run: |
          results=(
            "${{ needs.pr-title.result }}"
            "${{ needs.fmt.result }}"
            "${{ needs.clippy.result }}"
            "${{ needs.build-and-test.result }}"
            "${{ needs.publish-dry-run.result }}"
            "${{ needs.codegen-freshness.result }}"
            "${{ needs.integration.result }}"
          )
          for result in "${results[@]}"; do
            if [[ "$result" != "success" && "$result" != "skipped" ]]; then
              echo "One or more jobs failed: ${results[*]}"
              exit 1
            fi
          done
          echo "All CI checks passed"
